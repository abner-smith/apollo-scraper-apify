{
  "title": "Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "apolloSearchUrl": {
      "title": "Apollo search URL",
      "type": "string",
      "description": "Cole aqui a URL da busca no Apollo (com os filtros aplicados). Abra o Apollo, filtre, copie a URL e cole aqui.",
      "editor": "textfield"
    },
    "cookiesJson": {
      "title": "Cookies (JSON)",
      "type": "string",
      "description": "Cole aqui os cookies exportados do navegador em formato JSON (por exemplo via extensão).",
      "editor": "textarea"
    },
    "maxLeads": {
      "title": "Quantidade máxima de leads",
      "type": "integer",
      "description": "Número máximo de leads a coletar neste run.",
      "minimum": 1,
      "default": 150,
      "editor": "number"
    },
    "useApifyProxy": {
      "title": "Usar Apify Proxy",
      "type": "boolean",
      "description": "Ative para usar o proxy da Apify durante a navegação.",
      "default": true,
      "editor": "checkbox"
    },
    "notificationEmail": {
      "title": "E-mail para notificação (opcional)",
      "type": "string",
      "description": "E-mail para receber notificação ao finalizar (opcional).",
      "editor": "textfield"
    }
  },
  "required": ["apolloSearchUrl", "cookiesJson"]
}
