{
  "title": "Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "apolloSearchUrl": {
      "title": "Apollo search URL",
      "type": "string",
      "description": "Cole aqui a URL de busca do Apollo (com todos os filtros aplicados)."
    },
    "cookiesJson": {
      "title": "Cookies (JSON)",
      "type": "string",
      "description": "Cole aqui os cookies exportados (JSON)."
    },
    "maxLeads": {
      "title": "Quantidade máxima de leads",
      "type": "integer",
      "minimum": 1,
      "default": 150
    },
    "useApifyProxy": {
      "title": "Usar Apify Proxy",
      "type": "boolean",
      "default": true
    },
    "notificationEmail": {
      "title": "E-mail para notificação (opcional)",
      "type": "string",
      "description": "Caso queira receber notificação, informe um e-mail válido."
    }
  },
  "required": ["apolloSearchUrl", "cookiesJson"]
}
